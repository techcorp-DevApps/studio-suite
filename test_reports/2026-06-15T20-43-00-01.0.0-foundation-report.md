---
title: "Completion Report — TASK 01.0.0 studio-suite foundation (backend + web + Railway CD + CI)"
task: "01.0.0"
branch: "foundation/backend-web"
base: "main @ bbfbb4a"
date: "2026-06-15T20-43-00"
---

# TASK 01.0.0 — Foundation completion report

Stood up the studio-suite monorepo foundation: a FastAPI + async-Mongo backend
skeleton, a Vite + React 19 + Tailwind + shadcn/ui web skeleton, per-service Railway
configs, and a GitHub Actions CI pipeline. The skeleton **builds, runs, health-checks,
and is wired to deploy**. No auth, no R2 adapter, no media pipeline, no mobile — those
are later, separately-gated tasks.

All work is on `foundation/backend-web`, branched off `main` @ `bbfbb4a`. Every file
was staged by explicit path in per-concern commits (no `git add -A`).

## What was built

```
.gitignore  .gitattributes  README.md         # repo hygiene
mobile/README.md                               # reserved for task 01.1.0 (Expo/EAS)
backend/                                        # FastAPI + Motor skeleton
  requirements.txt  requirements-dev.txt  .python-version
  ruff.toml  pytest.ini
  config.py   # pydantic-settings; reserves R2_* fields by name (no adapter)
  db.py       # lazy Motor client; canonical MONGODB_URI / MONGODB_DB_NAME only
  server.py   # app factory; GET /api/health (liveness), GET /api/health/db (probe)
  tests/      # conftest (fresh_state autouse + mongomock_motor), health + db-config tests
web/                                            # Vite + React 19 + Tailwind + shadcn
  package.json  package-lock.json  .npmrc  .gitignore
  vite.config.js  postcss.config.js  tailwind.config.js
  components.json  jsconfig.json  eslint.config.js  index.html
  src/main.jsx  src/App.jsx  src/index.css
  src/lib/utils.js  src/components/ui/button.jsx  src/pages/Landing.jsx
backend/railway.toml   web/railway.toml         # INFRA (per-service)
.github/workflows/ci.yml                        # INFRA (backend + web jobs)
test_reports/2026-06-15T20-43-00-01.0.0-foundation-report.md
```

## Gate evidence (exact commands that prove each gate)

### ST1 — Repo hygiene  ·  PASS
- `git check-ignore web/node_modules web/build backend/.venv backend/.env web/.env`
  → all printed (ignored). `.gitignore` keeps `.env` / `node_modules/` / `.venv/` /
  `build/` out of git; `.env.example` templates are explicitly un-ignored.
- `grep -nE '^[A-Za-z_][A-Za-z0-9_]*=.+' backend/.env.example web/.env.example`
  → no matches (templates list var **names only**, no values).

### ST2 — Backend  ·  PASS
- `cd backend && ruff check .` → `All checks passed!`
- `cd backend && pytest -q` → `12 passed`
- `uvicorn server:app` started with **no** `MONGODB_URI`:
  - `GET /api/health`    → `200` `{"ok":true,"name":"Studio Suite API","environment":"development"}`
  - `GET /api/health/db` → `200` `{"ok":false,"status":"unreachable","error":"... 'MONGODB_URI' is not set ..."}`
    (probe reports status; the service does **not** crash when the DB is unreachable)
  - `GET /docs` → `200` (OpenAPI UI)
- Settings reserve `R2_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`,
  `R2_BUCKET_ORIGINALS`, `R2_BUCKET_DERIVATIVES` by name — no adapter wired.
- No `from __future__ import annotations` in any backend source
  (`grep -rn "^from __future__ import annotations" backend --include="*.py" | grep -v /.venv/` → none).

### ST3 — Web  ·  PASS
- `cd web && npm ci --include=dev && npm run build` → exit `0`; produced `build/index.html`
  + hashed `build/assets/*.css` (10.12 kB — Tailwind compiled locally) and `*.js`.
- `cd web && npm run lint` → exit `0` (ESLint clean).
- `npx --yes serve@14.2.6 -s build` →
  - `GET /`        → `200 text/html`
  - `GET /studio`  → `200` (SPA fallback serves index)
  - landing copy ("One studio platform…", "Enter the studio") present in the built JS bundle.
- Tailwind is locally built (PostCSS + `tailwindcss` + `autoprefixer`); no CDN; no
  `shadow-[var(--x)]` utilities. `vite.config.js` `allowedHosts` includes the Railway
  preview pattern `.up.railway.app`.

### ST4 — Deployment pipeline (INFRA)  ·  PASS
- `backend/railway.toml` parses; `startCommand = uvicorn server:app --host 0.0.0.0 --port $PORT`;
  `healthcheckPath = /api/health`.
- `web/railway.toml` parses; `buildCommand = "npm ci --include=dev && npm run build"`
  (contains `--include=dev`); `startCommand` serves `build/` via `serve`.
- `.github/workflows/ci.yml` parses; jobs `backend` (working-dir `backend`) and `web`
  (working-dir `web`); triggers on `pull_request` + `push` to `main`.

### ST5 — Completion report  ·  PASS
- This file, committed under `test_reports/`.

## ⚠ Infra files for owner review (not ordinary code)

Review these as infrastructure:

1. `backend/railway.toml` — FastAPI service build/start + health check.
2. `web/railway.toml` — static web service build (`--include=dev`) + `serve` start.
3. `.github/workflows/ci.yml` — backend (ruff + pytest) and web (eslint + build) jobs,
   required on PRs to `main`.

The owner provisions the two Railway services (one per directory), sets the real
environment variables (see each `.env.example`), and — if desired — enables branch
protection so both CI jobs are required before merge.

## Known issue (deferred — owner decision)

`npm audit` reports 4 advisories (2 low, 2 high), all in **dev/build tooling**
(`@eslint/plugin-kit` ReDoS via eslint; `esbuild` advisory via vite). These do **not**
ship in the production static bundle (eslint/esbuild are devDependencies; the deployed
artifact is the static `build/` served by `serve`). The available fixes are **major
breaking upgrades** (`vite@8`, `eslint@9.39`) that diverge from the owner's ratified
IlluminateMyGallery baseline (Vite 6 / React 19 / Tailwind 3). Deferred rather than
force-upgraded; raise as a baseline-bump decision for the owner.

## Explicitly deferred to later tasks

- **Auth** — studio admin portal + client portals (no auth logic in this task).
- **Cloudflare R2 adapter** — fields reserved by name only; no client wired.
- **Media pipeline** — R2 + Worker/CDN burst-access delivery.
- **Mobile** — Expo/EAS app + EAS Update OTA pipeline (task 01.1.0; `mobile/` reserved).

## Re-derivation aid

`git diff --stat origin/main HEAD` and the raw file list are included in the push
report so the orchestrator layer can re-derive before opening the PR.
